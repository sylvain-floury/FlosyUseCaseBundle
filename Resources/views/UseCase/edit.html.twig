{% extends 'FlosyUseCaseBundle::layout.html.twig' %}

{% block title %}
{{ 'views.usecase.edit.title'|trans({'%usecase%': entity.title}, 'FlosyUseCaseBundle') }}
{% endblock %}

{% block page %}

<h1>{{ 'views.usecase.edit.title'|trans({'%usecase%': entity.title}, 'FlosyUseCaseBundle') }}</h1>

<form class="well" action="{{ path('usecase_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <input type="hidden" name="_method" value="PUT" />
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-success">{{ 'views.common.actions.update'|trans({}, 'FlosyUseCaseBundle') }}</button>
    </p>
</form>

<div>
    <div class="float-left">
        <a class="btn" href="{{ path('usecase') }}">
            {{ 'views.common.actions.back_to_list'|trans({}, 'FlosyUseCaseBundle') }}
        </a>
    </div>
    <div class="float-left">
        <form action="{{ path('usecase_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit" onclick="return confirm('{{ 'views.usecase.edit.confirm_delete'|trans({}, 'FlosyUseCaseBundle') }}');">{{ 'views.common.actions.delete'|trans({}, 'FlosyUseCaseBundle') }}</button>
        </form>
    </div>
</div>
{% endblock %}