<h1>UseCase creation</h1>

<form action="{{ path('usecase_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('usecase') }}">
            Back to the list
        </a>
    </li>
</ul>
